apiVersion: dscinitialization.opendatahub.io/v2
kind: DSCInitialization
metadata:
  name: default-dsci
spec:
  monitoring:
    metrics:
      replicas: 1
      storage:
        retention: 90d
        size: 5Gi
